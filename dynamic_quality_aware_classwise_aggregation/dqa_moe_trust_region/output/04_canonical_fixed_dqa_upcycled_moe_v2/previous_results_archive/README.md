# Previous Results Archive

This directory keeps lightweight summaries from older DQA/MoE workspaces that
were removed to recover disk space. Heavy checkpoints, run folders, cached
datasets, and intermediate client states were intentionally deleted.

Kept per available experiment:

- `history.json`
- `manifest.json`
- round summary CSV
- `paper_protocol_eval_summary.{csv,md}`

The active experiment workspace remains:

- `04_canonical_fixed_dqa_upcycled_moe_v2`
